package token;

/** An enum TokenType represents a token type.
 Author: Renon Emilien.
 Date of last modification: 05/10/2024
 */
public enum TokenType {
    NUM,
    LET,
    ID,
    PRINT,
    IF,
    RELOP,
    GOTO,
    INPUT,
    OP,
    THEN,
    GOSUB,
    RETURN,
    END,
    REM,
    COMA,
    OPEN_PARENT,
    CLOSE_PARENT,
    CR,
    STR,
}
